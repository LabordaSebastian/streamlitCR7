name: Deploy FastAPI App on Merge to Develop

on:
  push:
    branches: ["develop"]

jobs:
  deploy:
    name: Deploy on local server
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Stop existing container
        run: docker compose down || true

      - name: Build and run new container
        run: docker compose -f streamlit/docker-compose.yml up -d --build

      - name: Notify on Discord (alternativa)
        uses: tsickert/discord-webhook@v5.0.0
        with:
          webhook-url: ${{ secrets.DISCORD_WEBHOOK }}
          content: "🚀 Deploy exitoso en servidor local"

